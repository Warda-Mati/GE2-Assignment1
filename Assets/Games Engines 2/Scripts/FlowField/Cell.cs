using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cell
{
    public Vector3 pos;
    public Vector2 index;

    public Cell(Vector3 pos, Vector2 index)
    {
        pos = this.pos;
        index = this.index;
    }
}
